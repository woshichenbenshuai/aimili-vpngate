services:
  aimili-proxy-bridge:
    image: alpine/socat:latest
    container_name: aimili-proxy-bridge
    restart: unless-stopped
    network_mode: host
    command:
      - -d
      - -d
      - TCP-LISTEN:18317,bind=172.17.0.1,fork,reuseaddr
      - TCP:127.0.0.1:8317
